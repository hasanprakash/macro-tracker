export enum ExerciseSource {
  HEALTH_CONNECT = 'health_connect',
  MANUAL = 'manual',
  GEMINI = 'gemini',
}

export enum CalculationMethod {
  HEALTH_PLATFORM = 'health_platform',
  MET = 'met',
  STEP_DISTANCE_ESTIMATE = 'step_distance_estimate',
}
